

## Consistent "I" Voice Pass

Seven files need pronoun updates from "we/our" to "I/my". No structural or logic changes — copy only.

### 1. `src/components/HeroSection.tsx`
- Replace the hero subheading paragraph with the shorter single-sentence version using "I help" and drop the second sentence entirely.

### 2. `src/components/Philosophy.tsx`
- Line 25: "can't" → "cannot", "At Gallag Works, we start" → "I start", "We understand" → "I understand", "We simplify" → "I simplify"
- Line 31: "We do the latter" → "I do the latter"

### 3. `src/components/ServicesSummary.tsx`
- Service 1: "I map how the work actually flows. Not the org chart version – the real one, with the invisible friction and manual workarounds exposed."
- Service 2: "I kill redundant steps..." (We → I)
- Service 3: "I engineer lightweight..." (We → I)

### 4. `src/components/FAQSection.tsx`
All six FAQ answers updated per the user's exact copy (security, tools, software, what-we-need, timeline, industries). "We" → "I" throughout, keeping "we are a fit" in the timeline answer.

### 5. `src/components/HowWeWork.tsx`
- Line 44: heading "How we work." → "How I work."
- Step descriptions stay as-is (imperative voice).

### 6. `src/components/EngagementTypes.tsx`
- Line 55: intro paragraph "We de-risk" → "I de-risk", "we move" → "I move"
- Tier 1 (line 17): "We expose" → "I expose", "We identify" → "I identify"
- Tier 2 (line 24): "We re-engineer" → "I re-engineer", "we don't just 'test' AI; we build" → "I don't just test AI; I build"
- Tier 3 (line 31): "We focus" → "I focus"

### 7. No changes to
- `LeakageEstimator.tsx`, `Contact.tsx` — already reader-addressed, no we/I issue.
- "Gallag Works" brand name references stay unchanged.

